//add whole repo - track all the files
git add .

// track a single file (add it to the ledger)
git add [filename]

// git commit - saves a version of your file (save a version, but not push up)
git commit -m "your message here"

// create a branch
git branch [branchname]

// check what branches are available
git branch

// checkout (or move to) another branch
git checkout [branchname]

// clone from a remote repo
git clone [address]

// push to repo (push files up)
git push origin [master or branch name]
// git push [name of remote] [branch name]

// pull from repo (make sure you are in correct directory)
git pull origin [branch name]
